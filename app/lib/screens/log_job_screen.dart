import 'package:flutter/material.dart';

class LogJobScreen extends StatelessWidget {
  const LogJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Log Job Screen Placeholder'),
      ),
    );
  }
}
